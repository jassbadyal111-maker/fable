const state={chats:[],activeChat:null,streaming:false,controller:null,config:null,theme:localStorage.getItem('fable-theme')||'system'};
window.fableState=state;
const $=id=>document.getElementById(id),chatList=$("chatList"),conversation=$("conversation"),input=$("input"),sendBtn=$("sendBtn");
const capabilityCache=new Map();
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]||c));
const escapeAttr=esc;
function tokenize(code,lang){const l=(lang||'').toLowerCase().trim();const aliases={py:'python',python3:'python',js:'javascript',mjs:'javascript',cjs:'javascript',ts:'typescript',tsx:'tsx',jsx:'jsx',html:'markup',xml:'markup',svg:'markup',sh:'bash',shell:'bash',yml:'yaml',md:'markdown'};return aliases[l]||l||'text'}
function highlight(code,lang){const language=tokenize(code,lang);let s=esc(code);const patterns={
 python:[[/(&quot;{3}[\\s\\S]*?&quot;{3}|&#039;{3}[\\s\\S]*?&#039;{3}|&quot;[^&]*?&quot;|&#039;[^&#039;]*?&#039;)/g,'str'],[/\b(def|class|import|from|return|if|elif|else|for|while|in|is|and|or|not|True|False|None|try|except|finally|with|as|async|await|yield|raise|lambda|pass|break|continue|global|nonlocal|assert|del)\b/g,'kw'],[/\b\d+(?:\.\d+)?\b/g,'num'],[/(^|\s)(#.*)$/gm,'com']],
 javascript:[[/(&quot;(?:\\.|[^&])*?&quot;|&#039;(?:\\.|[^&#039;])*?&#039;|`(?:\\.|[^`])*?`)/g,'str'],[/\b(const|let|var|function|return|if|else|for|while|new|class|extends|import|from|export|default|async|await|try|catch|finally|throw|typeof|instanceof|in|of|switch|case|break|continue|true|false|null|undefined|this|super|yield)\b/g,'kw'],[/\b\d+(?:\.\d+)?\b/g,'num'],[/(^|\s)(\/\/.*|\/\*[\s\S]*?\*\/)/gm,'com']],
 typescript:[[/(&quot;(?:\\.|[^&])*?&quot;|&#039;(?:\\.|[^&#039;])*?&#039;|`(?:\\.|[^`])*?`)/g,'str'],[/\b(interface|type|enum|namespace|implements|public|private|protected|readonly|as|const|let|var|function|return|if|else|for|while|new|class|extends|import|from|export|default|async|await|try|catch|throw|true|false|null|undefined|unknown|never|any|string|number|boolean|void)\b/g,'kw'],[/\b\d+(?:\.\d+)?\b/g,'num'],[/(^|\s)(\/\/.*|\/\*[\s\S]*?\*\/)/gm,'com']],
 bash:[[/(&quot;.*?&quot;|&#039;.*?&#039;)/g,'str'],[/\b(if|then|else|elif|fi|for|while|in|do|done|case|esac|function|export|local|return)\b/g,'kw'],[/(^|\s)(#.*)$/gm,'com'],[/\$\{?[A-Za-z_][A-Za-z0-9_]*\}?/g,'var']],
 json:[[/(&quot;(?:\\.|[^&])*?&quot;)(?=\s*:)/g,'prop'],[/(&quot;(?:\\.|[^&])*?&quot;)/g,'str'],[/\b(true|false|null)\b/g,'kw'],[/\b-?\d+(?:\.\d+)?\b/g,'num']],
 css:[[/([^{}]+)(?=\s*\{)/g,'sel'],[/([.#]?[A-Za-z_-][\w-]*)(?=\s*:)/g,'prop'],[/(&quot;.*?&quot;|&#039;.*?&#039;)/g,'str'],[/\b(important|inherit|initial|unset|flex|grid|block|inline|absolute|relative|fixed)\b/g,'kw'],[/(\/\*.*?\*\/)/gs,'com']],
 markup:[[/(&lt;!--[\s\S]*?--&gt;)/g,'com'],[/(&lt;\/?)([A-Za-z][\w:-]*)([^&]*?)(\/??&gt;)/g,'tag']],
 sql:[[/('(?:''|[^'])*')/g,'str'],[/\b(SELECT|FROM|WHERE|INSERT|INTO|VALUES|UPDATE|SET|DELETE|CREATE|ALTER|DROP|TABLE|JOIN|LEFT|RIGHT|INNER|OUTER|ON|AS|AND|OR|NOT|NULL|GROUP|BY|ORDER|HAVING|LIMIT|OFFSET|DISTINCT|CASE|WHEN|THEN|ELSE|END)\b/gi,'kw'],[/\b\d+(?:\.\d+)?\b/g,'num'],[/(--.*)$/gm,'com']],
 go:[[/(&quot;.*?&quot;|`.*?`)/g,'str'],[/\b(package|import|func|return|var|const|type|struct|interface|if|else|for|range|switch|case|default|go|defer|chan|select|map|nil|true|false)\b/g,'kw'],[/(^|\s)(\/\/.*)$/gm,'com']],
 rust:[[/(&quot;.*?&quot;)/g,'str'],[/\b(fn|let|mut|pub|impl|trait|struct|enum|use|mod|match|if|else|for|while|loop|return|self|Self|crate|async|await|move|const|static|true|false|None|Some)\b/g,'kw'],[/(^|\s)(\/\/.*)$/gm,'com']],
 java:[[/(&quot;.*?&quot;)/g,'str'],[/\b(public|private|protected|class|interface|extends|implements|static|final|void|int|long|double|float|boolean|new|return|if|else|for|while|try|catch|throw|throws|import|package|null|true|false)\b/g,'kw'],[/(^|\s)(\/\/.*|\/\*[\s\S]*?\*\/)/gm,'com']]
};
if(language==='markup')return s.replace(patterns.markup[0][0],'<span class="tok-com">$1</span>').replace(patterns.markup[1][0],(_,a,b,c,d)=>`<span class="tok-tag">${a}${b}${c}${d}</span>`);
const ps=patterns[language]||patterns.javascript;for(const [re,cls] of ps)s=s.replace(re,`<span class="tok-${cls}">$&</span>`);return s}
function markdown(text){const blocks=[];let source=String(text||'').replace(/\r\n/g,'\n');source=source.replace(/```([^\n]*)\n([\s\S]*?)(?:```|$)/g,(_,lang,code)=>{const id=blocks.length;const label=tokenize(code,lang);blocks.push(`<div class="code-wrap"><div class="code-head"><span>${esc(label)}</span><button class="copy-code" data-code="${encodeURIComponent(code)}">Copy</button></div><pre><code>${highlight(code,label)}</code></pre></div>`);return `\n@@CODE${id}@@\n`});source=esc(source).replace(/`([^`\n]+)`/g,'<code class="inline">$1</code>').replace(/\*\*(.+?)\*\*/g,'<strong>$1</strong>').replace(/\*([^*\n]+)\*/g,'<em>$1</em>').replace(/^### (.+)$/gm,'<h3>$1</h3>').replace(/^## (.+)$/gm,'<h2>$1</h2>').replace(/^# (.+)$/gm,'<h1>$1</h1>').replace(/^> (.+)$/gm,'<blockquote>$1</blockquote>').replace(/^(?:- |\* )(.+)$/gm,'<li>$1</li>').replace(/(<li>.*<\/li>\n?)+/g,m=>`<ul>${m}</ul>`);return source.split(/\n{2,}/).map(p=>/^<(h[1-3]|ul|blockquote|div)/.test(p.trim())?p:(p.trim()?`<p>${p.replace(/\n/g,'<br>')}</p>`:'')).join('').replace(/@@CODE(\d+)@@/g,(_,i)=>blocks[+i])}
function scrollBottom(){conversation.scrollTop=conversation.scrollHeight}
async function api(url,options={}){const r=await fetch(url,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options});if(!r.ok)throw Error(await r.text());return r.json()}
function selectedModel(){return localStorage.getItem('fable-model')||state.config?.default_model||''}
function currentModel(){return state.config?.models?.find(m=>m.id===selectedModel())||null}
async function hydrateModel(slug,rerender=true){if(!slug)return null;if(capabilityCache.has(slug)){const cap=capabilityCache.get(slug);mergeCapability(cap);if(rerender)renderModels();return cap}try{const cap=await api(`/api/model/${encodeURIComponent(slug)}`);capabilityCache.set(slug,cap);mergeCapability(cap);if(rerender)renderModels();return cap}catch{return null}}
function mergeCapability(cap){const model=state.config?.models?.find(m=>m.id===cap?.id);if(model&&cap)Object.assign(model,cap);}
function renderSidebar(){const q=($('chatSearch').value||'').trim().toLowerCase();const list=state.chats.filter(c=>!q||c.title.toLowerCase().includes(q));chatList.innerHTML=list.length?list.map(c=>`<div class="chat-row ${c.id===state.activeChat?'active':''}" data-id="${esc(c.id)}"><span class="chat-title">${esc(c.title)}</span><button class="chat-menu" data-menu="${esc(c.id)}" aria-label="Chat actions" title="Chat actions">•••</button></div>`).join(''):'<div class="empty-history">No conversations found</div>'}
function renderModels(){const options=$('modelOptions'),settings=$('settingsModel');options.innerHTML=state.config.models.map(m=>`<button class="model-option ${m.id===selectedModel()?'selected':''}" data-model="${esc(m.id)}"><span>${esc(m.name)}</span><small>${esc(m.id)}</small><b>${m.reasoning?'Reasoning':''}</b></button>`).join('');settings.innerHTML=state.config.models.map(m=>`<option value="${esc(m.id)}">${esc(m.name)}</option>`).join('');settings.value=selectedModel();$('modelName').textContent=currentModel()?.name||selectedModel();updateModelControls();updateComposerReasoning();}
async function chooseModel(model){localStorage.setItem('fable-model',model);$('modelName').textContent=model;renderModels();closeModelPopover();await hydrateModel(model,true);updateComposerReasoning();}
async function loadConfig(){state.config=await api('/api/config');if(!state.config.models.length)throw Error('No models available');if(!state.config.models.some(m=>m.id===selectedModel()))localStorage.setItem('fable-model',state.config.default_model);renderModels();await hydrateModel(selectedModel(),true);updateComposerReasoning()}
async function loadChats(){state.chats=await api('/api/chats');renderSidebar();if(!state.activeChat&&state.chats[0])await openChat(state.chats[0].id)}
function clearConversation(){conversation.innerHTML='<div class="welcome" id="welcome"><div class="welcome-mark">✳</div><h1>How can I help?</h1><p>What would you like to explore today?</p><div class="prompt-grid"><button class="prompt-card prompt-a" data-prompt="Help me write something"><span>✎</span><strong>Help me<br/>write something</strong></button><button class="prompt-card prompt-b" data-prompt="Explain a complex topic"><span>♧</span><strong>Explain a<br/>complex topic</strong></button><button class="prompt-card prompt-c" data-prompt="Summarize this for me"><span>▤</span><strong>Summarize<br/>this for me</strong></button><button class="prompt-card prompt-d" data-prompt="Help with coding"><span>&lt;/&gt;</span><strong>Help with<br/>coding</strong></button></div></div>';wirePrompts()}
async function openChat(id){const d=await api(`/api/chats/${id}`);state.activeChat=id;renderSidebar();conversation.innerHTML='';d.messages.forEach(m=>appendMessage(m.role,m.content,m.reasoning_summary));if(!d.messages.length)clearConversation();localStorage.setItem('fable-model',d.chat.model);renderModels();await hydrateModel(d.chat.model,true);closeSidebar();window.fableForceScroll?.()}
function wireCopy(root){root.querySelectorAll('.copy-code').forEach(b=>b.onclick=async()=>{try{await navigator.clipboard.writeText(decodeURIComponent(b.dataset.code));b.textContent='Copied';setTimeout(()=>b.textContent='Copy',1200)}catch{b.textContent='Copy'}})}
function reasoningMarkup(summary){if(!summary)return '';return `<details class="reasoning-box"><summary><span class="reasoning-dot">✦</span><span>Reasoning summary</span><span class="reasoning-hint">provider</span></summary><div class="reasoning-text">${markdown(summary)}</div></details>`}
function appendMessage(role,content,reasoningSummary=''){const w=document.createElement('div');w.className=`message ${role}`;w.innerHTML=role==='user'?`<div class="bubble">${esc(content)}</div>`:`<div class="assistant-content">${reasoningMarkup(reasoningSummary)}<div class="answer-body">${markdown(content)}</div></div>`;conversation.appendChild(w);wireCopy(w);return w}
function addStreamingMessage(){const w=document.createElement('div');w.className='message assistant';const model=currentModel();const effort=model?.reasoning?getReasoning(model):null;const reasoningPanel=model?.reasoning?`<details class="reasoning-box" open><summary><span class="reasoning-dot">✦</span><span>Reasoning</span><span class="reasoning-hint">${esc(effort||'active')}</span></summary><div class="reasoning-text"><span class="reasoning-live">Reasoning is active…</span><div class="reasoning-disclaimer">Public reasoning summary only. Private chain-of-thought is not exposed.</div></div></details>`:'';w.innerHTML=`<div class="assistant-content">${reasoningPanel}<span class="typing"><i></i><i></i><i></i></span><div class="answer-body"></div></div>`;conversation.appendChild(w);return w}
function setStreaming(on){state.streaming=on;sendBtn.innerHTML=on?'<span class="stop-icon" aria-hidden="true"></span>':'<span class="send-glyph" aria-hidden="true"></span>';sendBtn.classList.toggle('is-stop',on);sendBtn.setAttribute('aria-label',on?'Stop generating':'Send');sendBtn.title=on?'Stop generating':'Send'}
function updateReasoningPanelStatus(assistant,reasoning){const box=assistant?.querySelector('.reasoning-box');if(!box)return;const text=box.querySelector('.reasoning-text');if(reasoning){text.innerHTML=markdown(reasoning)+`<div class="reasoning-disclaimer">Provider-supplied reasoning summary. Private chain-of-thought is not exposed.</div>`}else{text.innerHTML='<span class="reasoning-live">Reasoning is active, but this route has not supplied a public reasoning summary yet.</span><div class="reasoning-disclaimer">The model can reason internally without returning its private chain-of-thought.</div>'}}
async function sendMessage(e){e?.preventDefault();if(state.streaming){state.controller?.abort();return}const text=input.value.trim();if(!text)return;if(!state.activeChat){const c=await api('/api/chats',{method:'POST',body:JSON.stringify({model:selectedModel()})});state.activeChat=c.id;state.chats.unshift(c);renderSidebar()}$('welcome')?.remove();appendMessage('user',text);input.value='';resizeInput();updateCount();window.fableForceScroll?.();const assistant=addStreamingMessage(),contentEl=assistant.querySelector('.answer-body');let answer='';let reasoning='';const model=currentModel();const effort=model?.reasoning?getReasoning(model):null;setStreaming(true);state.controller=new AbortController();try{const body={content:text,model:selectedModel(),max_tokens:getMaxTokens(model),system_prompt:localStorage.getItem('fable-system-prompt')??state.config.default_system_prompt};if(effort)body.reasoning_effort=effort;const r=await fetch(`/api/chats/${state.activeChat}/messages`,{method:'POST',headers:{'Content-Type':'application/json'},signal:state.controller.signal,body:JSON.stringify(body)});if(!r.ok||!r.body)throw Error(await r.text());const reader=r.body.getReader(),decoder=new TextDecoder();let buffer='';while(true){const {value,done}=await reader.read();if(done)break;buffer+=decoder.decode(value,{stream:true});const events=buffer.split('\n\n');buffer=events.pop()||'';for(const event of events){const line=event.split('\n').find(x=>x.startsWith('data: '));if(!line)continue;let d;try{d=JSON.parse(line.slice(6))}catch{continue}if(d.type==='reasoning_summary'){reasoning+=d.text||'';updateReasoningPanelStatus(assistant,reasoning);window.fableForceScroll?.()}if(d.type==='token'){answer+=d.text||'';contentEl.innerHTML=markdown(answer);wireCopy(assistant);window.fableForceScroll?.()}if(d.type==='done'&&d.reasoning_summary&&!reasoning){reasoning=d.reasoning_summary;updateReasoningPanelStatus(assistant,reasoning)}if(d.type==='error')throw Error(d.message)}}if(!answer)contentEl.innerHTML='<p class="stopped">Generation stopped.</p>';await loadChats()}catch(err){if(err.name!=='AbortError')contentEl.innerHTML=`<p class="error">Error: ${esc(err.message)}</p>`;else if(!answer)contentEl.innerHTML='<p class="stopped">Generation stopped.</p>'}finally{setStreaming(false);state.controller=null;window.fableForceScroll?.()}}
function getMaxTokens(model){const cap=Math.max(256,Number(model?.max_output)||Number(state.config?.default_max_tokens)||4096);let n=Number(localStorage.getItem('fable-max-tokens'));if(!Number.isFinite(n)||n<256)n=Math.min(4096,cap);return Math.min(Math.round(n),cap)}
function getReasoning(model){if(!model?.reasoning)return null;const levels=model.reasoning_levels||[];const v=localStorage.getItem(`fable-reasoning-${model.id}`)||model.reasoning_default||levels[0];return levels.includes(v)?v:null}
function updateModelControls(){const model=currentModel();if(!model)return;const cap=Math.max(256,Number(model.max_output)||Number(state.config?.default_max_tokens)||32768);const max=getMaxTokens(model);$('maxTokens').value=max;$('maxTokens').max=cap;$('tokenRange').max=cap;$('tokenRange').value=max;$('tokenRange').step=cap>=10000?256:1;$('tokenValue').value=max;$('tokenLimitLabel').textContent=`up to ${formatTokens(cap)}`;$('tokenScaleMax').textContent=formatTokens(cap);$('responseHelp').textContent=`Set maximum output tokens for ${model.name}`;const rs=$('reasoningSetting');if(model.reasoning&&model.reasoning_levels?.length){rs.classList.remove('hidden');const grid=$('effortGrid');const selected=getReasoning(model);grid.innerHTML=model.reasoning_levels.map(level=>`<button class="effort-btn ${level===selected?'active':''}" data-effort="${esc(level)}">${esc(level[0].toUpperCase()+level.slice(1))}</button>`).join('')}else{rs.classList.add('hidden');$('effortGrid').innerHTML=''}updateComposerReasoning()}
function updateComposerReasoning(){const wrap=$('composerReasoningWrap'),select=$('composerReasoning');if(!wrap||!select)return;const model=currentModel();if(!model?.reasoning||!(model.reasoning_levels||[]).length){wrap.classList.add('hidden');return}wrap.classList.remove('hidden');const selected=getReasoning(model);select.innerHTML=model.reasoning_levels.map(x=>`<option value="${esc(x)}">${esc(x[0].toUpperCase()+x.slice(1))}</option>`).join('');select.value=selected||model.reasoning_levels[0];select.title=`Reasoning effort: ${select.value}`}
function formatTokens(n){return n>=100000?`${Math.round(n/1000)}K`:n>=1000?`${Math.round(n/1000)}K`:String(n)}
function resizeInput(){input.style.height='auto';input.style.height=Math.min(input.scrollHeight,180)+'px'}function updateCount(){const n=input.value.length;$('charCount').textContent=`${n} / 200000`}
async function createNewChat(){if(state.streaming)return;const c=await api('/api/chats',{method:'POST',body:JSON.stringify({model:selectedModel()})});state.chats.unshift(c);state.activeChat=c.id;renderSidebar();clearConversation();closeSidebar();window.fableForceScroll?.()}
let pendingDelete=null;function closeModal(){pendingDelete=null;$('modalBackdrop').classList.add('hidden')}
async function deleteChat(){if(!pendingDelete)return;const id=pendingDelete;const btn=$('modalConfirm');btn.disabled=true;btn.textContent='Deleting…';try{await api(`/api/chats/${id}`,{method:'DELETE'});if(state.activeChat===id){state.activeChat=null;conversation.innerHTML='';clearConversation()}state.chats=state.chats.filter(c=>c.id!==id);renderSidebar()}finally{btn.disabled=false;btn.textContent='Delete';closeModal()}}
function confirmDelete(id){pendingDelete=id;const c=state.chats.find(x=>x.id===id);$('modalBackdrop').classList.remove('hidden');const title=$('modalBackdrop').querySelector('h2');const p=$('modalBackdrop').querySelector('p');if(title)title.textContent='Delete conversation?';if(p)p.textContent=`“${c?.title||'This conversation'}” will be permanently deleted. This action cannot be undone.`;$('modalConfirm').focus()}
async function renameChat(id){const c=state.chats.find(x=>x.id===id);if(!c)return;openRenameDialog(c)}
function openRenameDialog(c){let box=$('renameDialog');if(!box){box=document.createElement('div');box.id='renameDialog';box.className='modal-backdrop hidden';box.innerHTML=`<div class="modal rename-modal" role="dialog" aria-modal="true" aria-labelledby="renameTitle"><div class="modal-eyebrow">Conversation</div><h2 id="renameTitle">Rename chat</h2><p>Give this conversation a clear name.</p><label class="rename-field"><span>Chat name</span><input id="renameInput" maxlength="100" autocomplete="off"/></label><div class="modal-actions"><button class="modal-cancel" id="renameCancel">Cancel</button><button class="modal-primary" id="renameConfirm">Save name</button></div></div></div>`;document.body.appendChild(box);$('renameCancel').onclick=()=>box.classList.add('hidden');$('renameConfirm').onclick=saveRename;$('renameInput').addEventListener('keydown',e=>{if(e.key==='Enter')saveRename();if(e.key==='Escape')box.classList.add('hidden')})}box.dataset.id=c.id;$('renameInput').value=c.title;box.classList.remove('hidden');requestAnimationFrame(()=>$('renameInput').focus());}
async function saveRename(){const box=$('renameDialog'),id=box?.dataset.id,name=$('renameInput')?.value.trim();if(!id||!name)return;const btn=$('renameConfirm');btn.disabled=true;btn.textContent='Saving…';try{await api(`/api/chats/${id}`,{method:'PATCH',body:JSON.stringify({title:name})});const c=state.chats.find(x=>x.id===id);if(c)c.title=name;renderSidebar();box.classList.add('hidden')}finally{btn.disabled=false;btn.textContent='Save name'}}
let actionMenu=null;function closeActionMenu(){actionMenu?.remove();actionMenu=null}function openActionMenu(anchor,id){closeActionMenu();const menu=document.createElement('div');menu.className='chat-action-menu';menu.innerHTML=`<button data-action="rename"><span>✎</span>Rename</button><button data-action="delete" class="danger"><span>⌫</span>Delete</button>`;document.body.appendChild(menu);const r=anchor.getBoundingClientRect();menu.style.top=`${Math.min(window.innerHeight-100,r.bottom+4)}px`;menu.style.left=`${Math.min(window.innerWidth-180,Math.max(8,r.right-170))}px`;menu.querySelector('[data-action="rename"]').onclick=()=>{closeActionMenu();renameChat(id)};menu.querySelector('[data-action="delete"]').onclick=()=>{closeActionMenu();confirmDelete(id)};actionMenu=menu}
chatList.addEventListener('click',async e=>{const m=e.target.closest('[data-menu]');if(m){e.stopPropagation();openActionMenu(m,m.dataset.menu);return}const row=e.target.closest('.chat-row');if(row)await openChat(row.dataset.id)});
document.addEventListener('click',e=>{if(actionMenu&&!e.target.closest('.chat-action-menu')&&!e.target.closest('.chat-menu'))closeActionMenu()});
function wirePrompts(){conversation.querySelectorAll('[data-prompt]').forEach(b=>b.onclick=()=>{input.value=b.dataset.prompt;resizeInput();updateCount();input.focus()})}
function openSidebar(){document.body.classList.add('drawer-open');$('sidebar').classList.add('open');$('drawerBackdrop').classList.add('show')}function closeSidebar(){document.body.classList.remove('drawer-open');$('sidebar').classList.remove('open');$('drawerBackdrop').classList.remove('show')}
function openModelPopover(){const r=$('modelSwitch').getBoundingClientRect();$('modelPopover').style.top=`${r.bottom+8}px`;$('modelPopover').style.left=`${Math.max(12,r.left)}px`;$('modelPopover').classList.toggle('hidden');renderModels()}function closeModelPopover(){$('modelPopover').classList.add('hidden')}
$('newChatBtn').onclick=createNewChat;$('composer').onsubmit=sendMessage;input.addEventListener('input',()=>{resizeInput();updateCount()});input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendMessage(e)}});$('openSidebar').onclick=openSidebar;$('closeSidebar').onclick=closeSidebar;$('drawerBackdrop').onclick=closeSidebar;$('chatSearch').addEventListener('input',renderSidebar);$('modelSwitch').onclick=openModelPopover;$('modelOptions').addEventListener('click',async e=>{const b=e.target.closest('[data-model]');if(b)await chooseModel(b.dataset.model)});document.addEventListener('click',e=>{if(!e.target.closest('#modelSwitch')&&!e.target.closest('#modelPopover'))closeModelPopover()});$('settingsModel').addEventListener('change',e=>chooseModel(e.target.value));$('modalCancel').onclick=closeModal;$('modalConfirm').onclick=deleteChat;$('modalBackdrop').onclick=e=>{if(e.target.id==='modalBackdrop')closeModal()};$('effortGrid').addEventListener('click',e=>{const b=e.target.closest('[data-effort]');const m=currentModel();if(b&&m){localStorage.setItem(`fable-reasoning-${m.id}`,b.dataset.effort);updateModelControls()}});
function applyTheme(theme){state.theme=theme;localStorage.setItem('fable-theme',theme);const dark=theme==='dark'||(theme==='system'&&matchMedia('(prefers-color-scheme: dark)').matches);document.documentElement.classList.toggle('dark',dark);document.querySelectorAll('[data-theme]').forEach(b=>b.classList.toggle('active',b.dataset.theme===theme));$('topTheme').textContent=dark?'☼':'☾'}function toggleTheme(){applyTheme(document.documentElement.classList.contains('dark')?'light':'dark')}$('topTheme').onclick=toggleTheme;window.matchMedia('(prefers-color-scheme: dark)').addEventListener?.('change',()=>{if(state.theme==='system')applyTheme('system')});
function openSettings(){updateModelControls();$('maxTokens').value=getMaxTokens(currentModel());$('tokenValue').value=$('maxTokens').value;$('systemPrompt').value=localStorage.getItem('fable-system-prompt')??state.config.default_system_prompt;$('settingsModel').value=selectedModel();$('settingsBackdrop').classList.remove('hidden')}
function closeSettings(){$('settingsBackdrop').classList.add('hidden')}$('settingsBtn').onclick=openSettings;$('settingsClose').onclick=closeSettings;$('settingsBackdrop').onclick=e=>{if(e.target.id==='settingsBackdrop')closeSettings()};$('maxTokens').addEventListener('input',e=>{$('tokenValue').value=e.target.value;$('tokenRange').value=e.target.value});$('tokenRange').addEventListener('input',e=>{$('maxTokens').value=e.target.value;$('tokenValue').value=e.target.value});$('resetPrompt').onclick=()=>{$('systemPrompt').value=state.config.default_system_prompt};$('saveSettings').onclick=()=>{const m=currentModel();const cap=Math.max(256,Number(m?.max_output)||Number(state.config?.default_max_tokens)||4096);const n=Math.min(Math.max(256,Number($('maxTokens').value)||4096),cap);localStorage.setItem('fable-max-tokens',n);localStorage.setItem('fable-system-prompt',$('systemPrompt').value);localStorage.setItem('fable-model',$('settingsModel').value);renderModels();closeSettings()};document.querySelectorAll('[data-collapse]').forEach(b=>b.onclick=()=>{const target=$(b.dataset.collapse);target.classList.toggle('collapsed');b.querySelector('.collapse-icon').textContent=target.classList.contains('collapsed')?'⌄':'⌃'});document.querySelectorAll('[data-theme]').forEach(b=>b.onclick=()=>applyTheme(b.dataset.theme));
function setupComposerReasoning(){const right=document.querySelector('.composer-right');if(!right||$('composerReasoningWrap'))return;const wrap=document.createElement('label');wrap.id='composerReasoningWrap';wrap.className='composer-reasoning hidden';wrap.innerHTML='<span class="reasoning-mini-label">Think</span><select id="composerReasoning" aria-label="Reasoning effort"></select>';right.insertBefore(wrap,$('sendBtn'));$('composerReasoning').addEventListener('change',e=>{const m=currentModel();if(m)localStorage.setItem(`fable-reasoning-${m.id}`,e.target.value);updateComposerReasoning()})}
let deferredInstall=null;window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredInstall=e});$('installBtn').onclick=async()=>{if(deferredInstall){deferredInstall.prompt();await deferredInstall.userChoice;deferredInstall=null}else alert('Use your browser menu and choose “Install app” or “Add to Home Screen”.')};
if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('/sw.js'));applyTheme(state.theme);setupComposerReasoning();(async()=>{try{await loadConfig();await loadChats();wirePrompts();updateCount();updateComposerReasoning()}catch(e){conversation.innerHTML=`<div class="welcome"><h1>Fable failed to start</h1><p>${esc(e.message)}</p></div>`}})();
